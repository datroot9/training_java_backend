spring:
  datasource:
    url: jdbc:h2:mem:student_management_test;MODE=MySQL;NON_KEYWORDS=USER;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  sql:
    init:
      mode: always
  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: false

doma:
  dialect: h2

FRONTEND_URL: http://localhost

jwtSecret: 0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
jwtExpiration: 3600000
